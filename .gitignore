# Debian build artifacts
debian/.debhelper/
debian/debhelper-build-stamp
debian/files
debian/*.substvars
debian/*.log
debian/service-health/
debian/tmp/
obj-*/

# Build outputs (created in the parent directory)
*.deb
*.buildinfo
*.changes
*.dsc
*.tar.gz
*.tar.xz

# Editor / OS
*~
*.swp
.DS_Store
